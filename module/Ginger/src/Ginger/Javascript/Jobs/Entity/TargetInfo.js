var Entity = $CL.namespace('Ginger.Jobs.Entity');

$CL.require('Cl.Backbone.RelationalModel');

Entity.TargetInfo = function() {};

Entity.TargetInfo = $CL.extendClass(Entity.TargetInfo, Cl.Backbone.RelationalModel);